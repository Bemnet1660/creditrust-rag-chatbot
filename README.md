# CrediTrust RAG – Tasks 1 & 2

## Setup
1. Place your CFPB CSV file as data/complaints.csv.
2. Install dependencies: pip install -r requirements.txt

## Run Task 1 (EDA & Preprocessing)
`bash
python -m src.preprocess
